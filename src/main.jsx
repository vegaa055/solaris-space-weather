import React from 'react';
import ReactDOM from 'react-dom/client';
import SpaceWeatherDashboard from './SpaceWeatherDashboard.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SpaceWeatherDashboard />
  </React.StrictMode>
);
